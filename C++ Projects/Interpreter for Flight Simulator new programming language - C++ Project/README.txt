First part of First half of the big annual project - this project is an interpreter in c++ for new language of flight simulator app.
it works with the command design pattern with lexer parser and command to executes the commands
that we get from the flight simulator app(as a client - the app is the server)
Two participants with github.